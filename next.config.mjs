/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			"images.unsplash.com",
			"res.cloudinary.com",
			"cdn.pixabay.com",
			"cdn.statically.io",
			"images.pexels.com",
		],
	},
};

export default nextConfig;
